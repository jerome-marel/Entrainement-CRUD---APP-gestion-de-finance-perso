const express = require("express");
const router = express.Router();

const actionController = require("declarer ici le chemin de mon controler");

// déclarer ici les chemins necessaire à mon app

// router get  1   page home
//recupération de toutes les transaction du mois   entréé et sortie

// router get  2   page revenue
//router get 3 - page dépense

// router.post 1 - creation d'une nouvelle transaction

// router.patch 1 - modification d'une transaction  ou . put à verifier les différence

// router.delete 1 - suppression d'une transaction

module.exports = router;
